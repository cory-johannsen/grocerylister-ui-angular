'use strict';

describe('groceryLister.groceryLists module', function() {

  beforeEach(module('groceryLister.groceryLists'));

  describe('groceryLists controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var groceryListCtrl = $controller('GroceryListsCtrl');
      expect(groceryListCtrl).toBeDefined();
    }));

  });
});