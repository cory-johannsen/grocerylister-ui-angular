'use strict';

describe('groceryLister.groceryList module', function() {

  beforeEach(module('groceryLister.groceryList'));

  describe('groceryList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var groceryListCtrl = $controller('GroceryListCtrl');
      expect(groceryListCtrl).toBeDefined();
    }));

  });
});